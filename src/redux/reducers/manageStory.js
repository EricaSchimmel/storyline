export default function manageStory(state = {}, action) {
	switch (action.type) {
		case "ADD_STORY":
			break;

		case "REMOVE_STORY":
			break;

		case "ADD_COMMENT":
			break;

		default:
			return state;
	}
}
