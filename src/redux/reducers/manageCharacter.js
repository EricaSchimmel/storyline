export default function manageCharacter(state = {}, action) {
	switch (action.type) {
		case "ADD_CHARACTER":
			break;

		case "REMOVE_CHARACTER":
			break;

		case "ADD_COMMENT":
			break;

		default:
			return state;
	}
}
